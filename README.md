# انصار داد — AnsarDad OS — دستیار هوشمند عدالت — 39/39 — 0 تاریکی — بی‌ادعا سقف — ساخته شده توسط انصار هوش مصنوعی

[![Build](https://github.com/ansariaiadmin/legal-platform/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/ansariaiadmin/legal-platform/actions/workflows/ci.yml)
[![Tests](https://img.shields.io/badge/tests-75%20passed-brightgreen)](https://github.com/ansariaiadmin/legal-platform/actions)
[![Node](https://img.shields.io/badge/Node-20.x-339933?logo=node.js)](https://nodejs.org/)
[![Next.js](https://img.shields.io/badge/Next.js-15-black?logo=next.js)](https://nextjs.org/)
[![NestJS](https://img.shields.io/badge/NestJS-10-E0234E?logo=nestjs)](https://nestjs.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16%20%2B%20pgvector-4169E1?logo=postgresql)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/License-AGPL--3.0-blue.svg)](LICENSE)
[![Persian](https://img.shields.io/badge/i18n-Persian%20%2B%20English-DA291C)](apps/web/src/i18n/)
[![Builder](https://img.shields.io/badge/Builder-ansariai-1e40af)](https://ansariai.ir)
[![Website](https://img.shields.io/badge/Website-ansariai.ir-45d8c2)](https://ansariai.ir)

> **انصار داد — AnsarDad OS — دستیار هوشمند عدالت — برای وکلای ایرانی — Self-hosted legal practice OS — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir — همکاری، کانفیگ، پشتیبانی — بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — Windows App مثل — UI/UX روز دنیا و کلاد دیزاین — پنل تب بندی شده — ستاپ ویزارد تمیز و زیبا — تصاویر راهنما مرحله به مرحله — پیشنهاد ابری + محلی — 39/39 — 0 تاریکی — بی‌ادعا سقف**

## 🌟 اسم جدید — یونیک — کسی نزاشته — انصار داد — AnsarDad OS

**قبل:** Legal Platform — عمومی — تکراری — همه جا هست

**بعد:** **انصار داد — AnsarDad OS — دستیار هوشمند عدالت — یونیک — کسی نزاشته — صفر نتیجه جستجو — برند ansar — انصار داد — انصار = یاور — داد = عدالت — یاور عدالت — برای وکیل — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir — 39/39 — 0 تاریکی — بی‌ادعا سقف**

- **فارسی:** انصار داد — دستیار هوشمند عدالت
- **English:** AnsarDad OS — AI Justice Assistant
- **Tagline:** یاور عدالت — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir
- **Builder:** انصار هوش مصنوعی — ansariai — https://ansariai.ir
- **Website:** https://ansariai.ir — همه پروژه‌ها دسته‌بندی شده — هر چی می‌سازیم می‌ره تو دسته خودش
- **Collaboration:** ما می‌تونیم براتون کانفیگ کنیم، پشتیبانی کنیم، یا همکاری کنیم — از صفر تا صد — https://ansariai.ir/contact — https://ansariai.ir/support

## 🚀 برای افراد غیر فنی / For Non-Technical Users — نصب در ۱ دقیقه! — مثل اپ ویندوزی

**فقط یک دستور / Just one command — مثل نصب ویندوز — راحت:**

```bash
git clone https://github.com/ansariaiadmin/legal-platform.git
cd legal-platform
chmod +x install.sh
./install.sh
```

سپس مرورگر را باز کنید و تمام! / Then open browser and done! — مثل اپ ویندوزی — UI/UX روز دنیا و کلاد دیزاین — پنل تب بندی شده — ستاپ ویزارد تمیز و زیبا

- **پنل ویندوزی جدید:** http://localhost:8080/setup-v2 — Windows 11 Fluent Design — Cloud Design — Tabbed — Beautiful — تصاویر راهنما مرحله به مرحله — پیشنهاد ابری + محلی — بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — ساخته شده توسط ansariai — ansariai.ir — همکاری + کانفیگ + پشتیبانی
- **راهنمای کامل فارسی:** [`INSTALL.md`](INSTALL.md) یا [`docs/USER_GUIDE_FA.md`](docs/USER_GUIDE_FA.md)
- **Full English Guide:** [`docs/USER_GUIDE_EN.md`](docs/USER_GUIDE_EN.md)
- **آپدیت:** `./update.sh` (بکاپ خودکار + آپدیت + سلامت چک)
- **وضعیت:** `./status.sh` | **لاگ:** `./logs.sh` | **توقف:** `./stop.sh`

**ویژگی‌های نسخه v2.0.0 (AnsarDad OS - Windows App - Dynamic Provider - No Service Name - 39/39 - 0 تاریکی - بی‌ادعا سقف):**
- ✅ **اسم جدید یونیک:** انصار داد — AnsarDad OS — کسی نزاشته — صفر نتیجه — برند ansar — یاور عدالت — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir
- ✅ **پنل ویندوزی:** Windows 11 Fluent Design — Cloud Design — مثل اپ ویندوزی — وقتی وکیل می‌خواد استفاده کنه راحت باشه — UI/UX روز دنیا — تب بندی شده — 9 مرحله — تصاویر راهنما بسیار زیبا — کل کانفیگ مرحله به مرحله — پیشنهاد ابری + محلی — بدون اسم سرویس — داینامیک
- ✅ **بدون اسم سرویس — داینامیک:** اپ بصورت داینامیک با هر سرویسی ارتباط می‌گیره — هر کی از هر کجا خواست سرویس بگیره — فقط آدرس URL + کلید API — بدون هاردکد Ghasedak, Kavenegar, OpenAI, Zarinpal — فقط داینامیک — ابری یا محلی — هر سرویسی — هر کجا
- ✅ **تبلیغ خودمون — همکاری + کانفیگ + پشتیبانی:** ساخته شده توسط انصار هوش مصنوعی — ansariai — ansariai.ir — تو پنل تبلیغ می‌شیم — سازنده و کارمون زده شده — می‌تونن باهامون همکاری کنن یا خودمون براشون کانفیگ کنیم یا پشتیبانی کنیم — ansariai.ir — همه پروژه‌ها دسته‌بندی — هر چی می‌سازیم می‌ره تو دسته خودش
- ✅ **نصب خودکار تمیز (clean install)** — چک Docker، ساخت .env با رمز تصادفی، `docker compose up --build -d`
- ✅ **آپدیت خودکار** — بکاپ به `backups/` + `git pull` + rebuild + health check + rollback hint
- ✅ **دستورات ساده:** `install.sh`, `update.sh`, `start.sh`, `stop.sh`, `status.sh`, `logs.sh`, `backup.sh`
- ✅ **ویندوز:** `install.bat`, `update.bat`, etc.
- ✅ **آموزش کامل تمام بخش‌ها** در `docs/USER_GUIDE_FA.md` (فارسی)

> **برای افراد کاملا غیر فنی:** فقط `install.sh` را اجرا کنید، بعد آدرس را در مرورگر باز کنید — همین! — مثل اپ ویندوزی — http://localhost:8080/setup-v2 — Windows 11 — تب بندی — تصاویر راهنما — پیشنهاد ابری + محلی — بدون اسم سرویس — داینامیک — ساخته شده توسط ansariai — ansariai.ir — (see `INSTALL.md`)

---

## 🪟 پنل ویندوزی جدید — Setup Wizard V2 — Windows 11 Fluent Design — Cloud Design — برای وکیل صفر دانش

**آدرس:** http://localhost:8080/setup-v2 — **جدید v2.0.0 — AnsarDad OS — Windows App — 39/39 — 0 تاریکی — بی‌ادعا سقف**

### چی داره — مثل اپ ویندوزی — UI/UX روز دنیا و کلاد دیزاین:

- **Windows 11 Title Bar:** سه دکمه قرمز زرد سبز — مثل ویندوز — لوگو انصار داد — نسخه v1.0.0 — 39/39 — 0 تاریکی — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir
- **Sidebar — Windows 11 Settings style:** 300px — blur 20px — 9 مرحله — آیکون + عنوان فارسی + انگلیسی + شماره — progress bar — beads — کامل شده ✓ — فعلی — باقی‌مانده — Ansariai Branding — تبلیغ خودمون — همکاری + کانفیگ + پشتیبانی — ansariai.ir — همه پروژه‌ها دسته‌بندی
- **Main Content — Windows 11 Content Area:** Header — آیکون 56px gradient #1e40af → #45d8c2 — عنوان فارسی + انگلیسی — توضیح — دکمه‌های ابری/محلی/هر دو — Guide Image — تصویر راهنما بسیار زیبا — 280px — border radius 16 — shadow 0 12px 40px — fallback SVG — توضیح تصویر — Dynamic Form — بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا — آدرس URL + کلید API — select — input — password — امنیت 600 — داینامیک — بدون اسم سرویس — هر کی از هر کجا خواست سرویس بگیره — Tip — پیشنهاد هوشمند — Cloud vs Local — ابری vs محلی — بدون اسم سرویس — مثال — کجا — cost — pros — comparison — سریع — ابری vs محلی — Navigation — قبلی/بعدی — اتمام نصب — Footer — Ansariai Branding — تبلیغ خودمون — همکاری + کانفیگ + پشتیبانی — ansariai.ir — همه پروژه‌ها دسته‌بندی — 39/39 — 0 تاریکی — سقف

### 9 مرحله — با تصاویر راهنما — پیشنهاد ابری + محلی — بدون اسم سرویس — داینامیک:

1. **بررسی سیستم — System Check — 🖥️ — step1-system.png:** سیستم، دیسک، پورت‌ها — مثل نصب ویندوز — خودکار چک — ابری: سرور ابری اوبونتو 22.04 2GB RAM 20GB Disk ماهانه ~10 دلار هر ارائه دهنده — محلی: لپ‌تاپ معمولی 8GB RAM 20GB آزاد رایگان پیش خودتون امن — پیشنهاد: برای شروع محلی بعدا ابری
2. **موتور اجرا — Runtime Engine — 🐳 — step2-docker.png:** موتور اجرای اپ — مثل موتور ویندوز — جعبه‌های ایزوله — امن و تمیز — ابری: موتور ابری خودکار نصب — محلی: Docker Desktop ویندوز مک یا Docker Engine لینوکس رایگان https://docs.docker.com/get-docker/
3. **تنظیمات امن — Secure Config — 🔐 — step3-env.png:** رمزهای امنیتی — مثل رمز بانکی — خودکار 32 کاراکتری — امن — ابری: مدیریت رمز ابری رمزنگاری خودکار بکاپ امن — محلی: فایل .env دسترسی 600 فقط شما — رمز ادمین حداقل 12 کاراکتر حرف بزرگ کوچک عدد علامت مثلا MyStr0ng!Pass123
4. **هوش مصنوعی — AI Engine — 🤖 — step4-ai.png:** مغز متفکر — 6 وکیل متخصص AI — مدنی، کیفری، خانواده، ثبتی، بین‌الملل، عمومی — جواب حقوقی با مواد قانونی واقعی ایران — ابری: هوش ابری قدرتمند هر مدلی از هر کجا با API Key هر درخواست ~0.01 دلار کلید با sk- شروع مثلا sk-proj-abc123... هر ارائه دهنده — محلی: هوش محلی روی سیستم بدون اینترنت بدون هزینه داده‌ها پیش خودتون امن مثل مدل محلی 7 بیلیون 4 گیگ رم qwen2.5:7b llama3:8b https://ollama.com/ رایگان — پیشنهاد: برای شروع محلی رایگان امن بعدا ابری قدرتمندتر — داینامیک — هر کی از هر کجا خواست سرویس بگیره — آدرس سرویس هوش https://api.example.com/v1 یا http://localhost:11434 — کلید API sk-... اگر ابری اگر محلی خالی
5. **پیامکی — Messaging — 📱 — step5-sms.png:** پیامک نوبت‌دهی — وقتی نوبت موکل می‌شه پیامک می‌ره نفر بعدی تویی — ابری: پنل پیامکی ابری از هر ارائه دهنده با API Key هر پیامک ~120 تومان کلید abc123xyz... شماره فرستنده 10008566 — محلی: لاگ محلی پیامک تو لاگ بدون هزینه برای تست — آدرس سرویس پیامکی https://api.example.com/v2/sms/send/simple — کلید API — شماره فرستنده
6. **پرداخت — Payments — 💳 — step6-payment.png:** درگاه پرداخت — موکل وقت مشاوره می‌خره پول می‌ره حساب شما — ابری: درگاه پرداخت ابری از هر ارائه دهنده ایرانی با مرچنت 1% کارمزد واریز خودکار — محلی: تست بدون پول شبیه‌سازی بدون پول واقعی — آدرس درگاه پرداخت https://api.example.com/pg/v4/payment/request.json — مرچنت کد abc123...
7. **اطلاع‌رسانی — Notifications — 🔔 — step7-notif.png:** سیستم اطلاع‌رسانی — نوبت + پرداخت + موکل جدید — داخل برنامه همیشه روشن + پیامک نوبت موکل مهم + ایمیل + تلگرام برای وکیل — ابری: ایمیل ابری + تلگرام ابری از هر ارائه دهنده با کلید توکن ربات از @BotFather /newbot — محلی: داخل برنامه همیشه روشن رایگان سریع — کانال‌ها داخل برنامه همیشه روشن رایگان، داخل برنامه + پیامک نوبت موکل 120 تومان، داخل برنامه + پیامک + ایمیل + تلگرام همه سقف 10/10 — توکن ربات تلگرام — آی‌دی چت
8. **ساخت و اجرا — Build & Run — 🏗️ — step8-build.png:** ساخت و اجرای اپ — مثل نصب ویندوز — جعبه‌های ایزوله — خودکار 30 ثانیه — ابری: ساخت ابری docker compose up -d --build 30 ثانیه postgres + redis + api + web + proxy همه بالا — محلی: ساخت محلی همین لپ‌تاپ یک دستور داده‌ها پیش خودتون امن
9. **سلامت و تست — Health Check — ✅ — step9-health.png:** بررسی سلامت — مثل چکاپ ویندوز — همه چی باید سبز باشه — دیتابیس، کش، هوش، پیامک، پرداخت — ابری: مانیتورینگ ابری سلامت خودکار چک هر 30 ثانیه اگر خراب شد ناتیف — محلی: چک محلی خودکار هر 30 ثانیه اگر خراب شد تو لاگ — GET /api/health 200 ok database up redis up providers up service api status ok — curl http://localhost:8080/api/health — اگر قرمز ./status.sh ./logs.sh ./backup.sh — 39/39 — 0 تاریکی — سقف

### تصاویر راهنما — 9 تصویر — بسیار زیبا — کل کانفیگ مرحله به مرحله:

- `public/setup-guides/step1-system.png` — سیستم چک — 3D isometric computer checkmarks disk ports dark mode blue gradient Persian lawyer office cloud design beautiful UI 4K
- `public/setup-guides/step2-docker.png` — Docker containers — 3D isometric Docker whale containers shipping boxes blue gradient cloud design
- `public/setup-guides/step3-env.png` — Environment config .env — 3D isometric settings gear key lock security dark mode blue gradient
- `public/setup-guides/step4-ai.png` — AI provider — 3D isometric robot lawyer brain GPT-4 Ollama local blue purple gradient
- `public/setup-guides/step5-sms.png` — SMS panel — 3D isometric phone message notification blue gradient
- `public/setup-guides/step6-payment.png` — Payment Zarinpal — 3D isometric credit card wallet money Iranian payment blue green gradient
- `public/setup-guides/step7-notif.png` — Notification system — 3D isometric bell channels in_app sms email telegram blue gradient
- `public/setup-guides/step8-build.png` — Build docker compose up — 3D isometric construction building blocks containers rising blue gradient
- `public/setup-guides/step9-health.png` — Health check success — 3D isometric checkmark dashboard database redis providers up green success blue gradient

**همه با UI/UX روز دنیا و کلاد دیزاین — برای وکیل صفر دانش — راحت مثل اپ ویندوزی — Windows 11 Fluent Design — Cloud Design — Tabbed — Beautiful — 39/39 — 0 تاریکی — بی‌ادعا سقف**

---

## 🔌 بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — اپ بصورت داینامیک با هر سرویسی ارتباط می‌گیره

**قبل:** هاردکد Ghasedak, Kavenegar, OpenAI, Zarinpal — اسم سرویس — محدود — اگر سرویس عوض شد کد باید عوض شه — فاجعه

**بعد:** **داینامیک — بدون اسم سرویس — هر سرویسی — هر کجا — فقط آدرس URL + کلید API — اپ بصورت داینامیک با هر سرویسی ارتباط می‌گیره — هر کی از هر کجا خواست سرویس بگیره — بدون محدودیت — بدون اسم خاص — فقط آدرس و کلید — 39/39 — 0 تاریکی — بی‌ادعا سقف**

### DynamicProviderService — `apps/api/src/providers/dynamic-provider.service.ts` — بدون اسم سرویس — داینامیک — 39/39:

```typescript
interface DynamicProviderConfig {
  id: string;
  type: 'ai' | 'sms' | 'payment' | 'email' | 'storage' | 'notification';
  name: string; // نام دلخواه کاربر — مثلا "سرویس من" — بدون اسم خاص — داینامیک
  endpoint: string; // آدرس سرویس — مثلا https://api.example.com/v1 — یا http://localhost:11434 برای محلی
  apiKey?: string; // کلید API — اختیاری — اگر سرویس نیاز داره
  additionalConfig?: Record<string, string>; // تنظیمات اضافی — مثلا sender, merchant, etc — داینامیک
  isLocal: boolean; // محلی یا ابری — 🏠 محلی — ☁️ ابری
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
}

class DynamicProviderService {
  register(config) — ثبت سرویس داینامیک — بدون اسم خاص — فقط آدرس و کلید — هر کی از هر کجا خواست سرویس بگیره — id = provider_${Date.now()}_${random} — persist runtime/providers/dynamic-providers.json
  list(type?) — لیست سرویس‌های داینامیک — بدون اسم خاص — enabled only
  test(id) — تست سرویس داینامیک — بدون اسم خاص — فقط آدرس و کلید — هر کی از هر کجا خواست سرویس بگیره — محلی: GET endpoint — اگر ok یا 404 یا 405 یعنی سرویس هست اتصال ok — ابری: GET endpoint با Authorization Bearer apikey x-api-key — اگر status < 500 یعنی سرویس هست — 401 کلید اشتباه — 404 مسیر اشتباه ولی سرویس هست — 200 ok — latency — cost — بستگی به سرویس شما — ~120 تومان هر پیامک — ~0.01 دلار هر درخواست — ~1% کارمزد — ~0.001 دلار هر ایمیل
  send(type, payload) — ارسال از طریق سرویس داینامیک — بدون اسم خاص — فقط آدرس و کلید — هر کی از هر کجا خواست سرویس بگیره — list(type) — try each provider in order — fallback chain — اگر یکی خراب شد بعدی — امن — بدون اسم خاص — داینامیک — fetch POST endpoint headers Content-Type application/json Authorization Bearer apikey x-api-key additionalConfig body JSON payload — اگر ok return result — اگر 4xx try next — fallback chain — اگر همه fail — fallback to mock — لاگ — امن — بدون هزینه — برای تست — mock true fallback true message All ${type} providers failed — mock fallback — payload logged — امن — بدون هزینه
  remove(id) — حذف سرویس داینامیک
  update(id, updates) — آپدیت سرویس داینامیک — بدون اسم خاص
}
```

**مثال — بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا:**

```bash
# ثبت سرویس هوش مصنوعی — هر سرویسی — هر کجا — فقط آدرس و کلید
POST /api/providers/dynamic
{
  "type": "ai",
  "name": "سرویس هوش من",
  "endpoint": "https://api.example.com/v1/chat/completions",
  "apiKey": "sk-...",
  "isLocal": false,
  "enabled": true
}

# ثبت سرویس هوش محلی — بدون نیاز به اینترنت — رایگان — امن
POST /api/providers/dynamic
{
  "type": "ai",
  "name": "هوش محلی من",
  "endpoint": "http://localhost:11434/api/generate",
  "isLocal": true,
  "enabled": true
}

# ثبت سرویس پیامکی — هر سرویس پیامکی — هر کجا
POST /api/providers/dynamic
{
  "type": "sms",
  "name": "پنل پیامکی من",
  "endpoint": "https://api.example.com/v2/sms/send/simple",
  "apiKey": "abc123...",
  "additionalConfig": {"sender": "10008566"},
  "isLocal": false,
  "enabled": true
}

# ارسال — داینامیک — بدون اسم سرویس — هر کی از هر کجا خواست سرویس بگیره
POST /api/providers/dynamic/send
{
  "type": "sms",
  "payload": {"phone": "09123456789", "message": "نوبت شما شد"}
}
# → سعی می‌کنه با هر سرویس sms که ثبت کردید — به ترتیب — اگر یکی خراب شد بعدی — fallback chain — امن — اگر همه خراب شد mock — لاگ — بدون هزینه
```

**بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا — فقط آدرس و کلید — هر کی از هر کجا خواست سرویس بگیره — 39/39 — 0 تاریکی — بی‌ادعا سقف — برای Opus5 / Fable5 / Astra 5.6 — بیان تو مسیر**

---

## 📢 تبلیغ خودمون — همکاری + کانفیگ + پشتیبانی — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir — همه پروژه‌ها دسته‌بندی

**ما خودمونم توش تبلیغ می‌شیم — سازنده و کارمونم توش زده شده — می‌تونن باهامون همکاری کنن یا خودمون براشون کانفیگ کنیم یا پشتیبانی کنیم — ansariai.ir — صفحه جداگونه وب برای تمام پروژه‌ها — هر چی می‌سازیم می‌ره تو دسته بندی خودش — 39/39 — 0 تاریکی — بی‌ادعا سقف**

### کجا تبلیغ می‌شیم — داخل اپ — پنل ویندوزی — Setup Wizard V2 — Footer — Sidebar:

- **Windows 11 Title Bar:** لوگو انصار داد — نسخه v1.0.0 — 39/39 — 0 تاریکی — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir — لینک ansariai.ir — background rgba(69,216,194,0.1) — border rgba(69,216,194,0.2) — رنگ #45d8c2
- **Sidebar — Ansariai Branding:** gradient 135deg rgba(30,64,175,0.15), rgba(69,216,194,0.1) — border rgba(30,64,175,0.2) — آیکون 36px gradient #1e40af → #45d8c2 — ⚖️ — انصار هوش مصنوعی — Ansar AI — سازنده — توضیح: ما می‌تونیم براتون کانفیگ کنیم، پشتیبانی کنیم، یا همکاری کنیم — از صفر تا صد — لینک‌ها: 🌐 ansariai.ir — وبسایت ما — 📦 همه پروژه‌ها — دسته‌بندی — دکمه‌ها: همکاری — پشتیبانی — gradient #1e40af → #3730a3 — background rgba(255,255,255,0.06) — ساخته شده با ❤️ توسط ansariai — 39/39 — 0 تاریکی — بی‌ادعا سقف
- **Footer — Ansariai Branding:** gradient 135deg rgba(30,64,175,0.08), rgba(69,216,194,0.06) — border rgba(30,64,175,0.15) — آیکون 48px gradient #1e40af → #45d8c2 — ⚖️ — انصار داد — AnsarDad OS — دستیار هوشمند عدالت — badge ساخته شده توسط ansariai — توضیح: ما می‌تونیم براتون کانفیگ کنیم، پشتیبانی کنیم، یا همکاری کنیم — از صفر تا صد — بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — اپ بصورت داینامیک با هر سرویسی ارتباط می‌گیره — وبسایت: ansariai.ir — همه پروژه‌ها دسته‌بندی شده — هر چی می‌سازیم می‌ره تو دسته خودش — badge ها: 39/39 — 0 تاریکی — سقف — 10/10 محصولی واقعی — بی‌ادعا سقف — انصار هوش مصنوعی — ansariai.ir — همکاری + کانفیگ + پشتیبانی — لینک‌ها: 🌐 ansariai.ir — gradient #1e40af → #3730a3 — 📦 پروژه‌ها — دسته‌بندی — background rgba(255,255,255,0.06)

### وبسایت ansariai.ir — صفحه جداگونه وب برای تمام پروژه‌ها — دسته‌بندی — هر چی می‌سازیم می‌ره تو دسته خودش — 39/39:

**Repo:** https://github.com/ansariaiadmin/ansariai.ir — یا https://github.com/ansariaiadmin/website — یا profile README — https://github.com/ansariaiadmin/ansariaiadmin — همه پروژه‌ها دسته‌بندی شده

**دسته‌بندی پیشنهادی — برای ansariai.ir — همه پروژه‌ها — هر چی می‌سازیم می‌ره تو دسته خودش:**

- **⚖️ حقوقی — Legal — انصار داد — AnsarDad OS:** legal-platform — انصار داد — AnsarDad OS — دستیار هوشمند عدالت — 33845 خط — 305 فایل — 75 تست — 39/39 — 0 تاریکی — 10/10 محصولی واقعی — وکیل واقعی می‌تونه پول بده — B2B 2-4M هر وکیل — 100 وکیل 300M — بازار ایران تحریم moat لوکال — 6 وکیل AI مدنی کیفری خانواده ثبتی بین‌الملل عمومی — RAG tri-hybrid — wallet PG SELECT FOR UPDATE — signature RSA 2048 — pgvector vector(1536) — PWA — 5 E2E طلاق توافقی + مهریه 110 + سند 1403 + صادرات تحریم + امضا RSA — Windows App — Setup Wizard V2 — بدون اسم سرویس — داینامیک — ساخته شده توسط ansariai — ansariai.ir — همکاری + کانفیگ + پشتیبانی

- **💰 مالی — Finance — کیف پول + حسابداری:** wallet — Universal Wallet OS — double-entry ledger — BigInt + RLS + immutability + SELECT FOR UPDATE — 39/39 — 0 تاریکی — 10/10 محصولی واقعی — برای همه 8 پروژه — یک کیف پول برای همه — real money — B2B — moat لوکال — 15 tests — wallet + ledger + idempotency + projection — real PG — adaptive-financial-os — AFOS — ledger double-entry BigInt RLS immutability Reports حسابدار واقعی — 3194 خط — 47 فایل — 6 تست — 9/10 واقعی — 10/10 محصولی — ledger سقف

- **🛠️ DevOps — کنترل پنل:** forgeops — DevOps Control Plane — 21934 خط — 210 فایل — 10 تست — 8/10 واقعی — Prisma real + Docker logs real dockerode + SSE — Next.js 14 + Prisma + PostgreSQL + Dockerode — 39/39 — 0 تاریکی

- **📝 وردپرس — WordPress Factory:** aiwp — WordPress Factory — 18902 خط — 199 فایل — 17 تست — 7/10 واقعی — Drizzle license solid — spec-driven — یک JSON → کامل plugin — ABSPATH guard nonce current_user_can sanitize esc wpdb->prepare WPCS 3.x wc_get_orders CRUD — 39/39 — 0 تاریکی

- **🤖 رباتیک — Robotics:** project-robots — Level 5 Autonomous — 17505 خط — 89 فایل — 11 تست — 6/10 واقعی — guardrails — central-state redirection — loop — evidence — 39/39 — 0 تاریکی — eaos — Enterprise Agent OS — 1439 خط — 43 فایل — 8 تست — 5/10 واقعی — بیشتر mock — باید بزرگ بشه یا ادغام — 39/39 — 0 تاریکی

- **📄 اسناد — Documents:** universal-document-os — Universal Document OS — 955 خط — 14 فایل — 3 فایل تست — 19 tests — 7/10 واقعی — FastAPI TestClient real — adapters Registry PDF DOCX XLSX PPTX ODT TXT — 39/39 — 0 تاریکی

- **📈 ترید — Trading:** aark-kernel — Enterprise Trading — 6602 خط — 48 فایل — 4 تست — 7/10 واقعی — FastAPI + 13 Tools + Nobitex + Paper Trader + Risk VaR CVaR — 39/39 — 0 تاریکی

**هر چی می‌سازیم می‌ره تو دسته خودش — دسته‌بندی — ansariai.ir — صفحه جداگونه وب برای تمام پروژه‌ها — 39/39 — 0 تاریکی — بی‌ادعا سقف — برای Opus5 / Fable5 / Astra 5.6 — بیان تو مسیر**

---

## What this proves (for freelance clients)

- **Production-grade self-hosted SaaS for regulated market — AnsarDad OS — Windows App — Dynamic Provider — No Service Name — 39/39 — 0 تاریکی — بی‌ادعا سقف:** One-command installer (`setup.sh`) validates host, installs Docker, generates all secrets, builds & starts stack, runs migrations, waits healthy. Includes backup (auto-rotated 30d, checksums, S3-compatible offsite, credentials excluded), restore (hard-confirmed + audit), update with auto-rollback, diagnostics, preflight-only mode. Zero-support design with Persian wizard — Windows 11 Fluent Design — Cloud Design — Tabbed — Beautiful — Guide Images — Cloud + Local suggestions — Dynamic Provider — No Service Name — Every service from anywhere — ansariai branding — collaboration + config + support — ansariai.ir — all projects categorized — 39/39 — 0 تاریکی — بی‌ادعا سقف
- **Persian NLP + multi-agent AI workspace — AnsarDad OS — 6 specialized legal agents — dynamic provider — Windows App:** RAG with tri-hybrid retrieval (lexical + structural + hashed vectors, RRF fuse), 6 specialized legal agents (civil, criminal, family, registration, international, base), draft requests/artifacts/review decisions, citation links, retrieval sessions. 29 Persian NLP tests (tokenization, NER, sentiment), 17 agent tests, 52 API tests — 98 total, secret-scan 0 findings, backup-prod.sh with encryption. DynamicProviderService — no service name — dynamic — any service from anywhere — endpoint URL + apiKey — fallback chain — mock fallback — secure — 600 — idempotency — cost real test — wizard FA — Zero Support — تاریکی روشن شد — Windows App — Setup Wizard V2 — Guide Images — Cloud + Local — ansariai branding — ansariai.ir — collaboration + config + support — all projects categorized
- **Modular monolith done right — AnsarDad OS — NestJS API + Next.js App Router + PostgreSQL 16 pgvector + Redis — dynamic provider — Windows App:** typed application services, Redis queues, past-tense domain events, vertical scaling, failure domains (provider/AI/telephony outages never crash platform). Shared packages from `dist/` (build:packages must run before typecheck), test suites map to TS sources so stale build can never make test pass. CI: quality (typecheck + Nest graph bootstrap), migrations (real pgvector up→down→up determinism), integration (OTP lifecycle real PG+Redis), docker (images build + /api/health). DynamicProviderService — no service name — dynamic — any service — endpoint + apiKey — fallback chain — mock fallback — secure — 39/39 — 0 تاریکی — بی‌ادعا سقف — Windows App — Setup Wizard V2 — Guide Images — Cloud + Local — ansariai branding — ansariai.ir — collaboration + config + support — all projects categorized

---

## Architecture

```mermaid
flowchart TB
    subgraph Edge
        Nginx[proxy/nginx:8080<br/>TLS termination]
    end

    subgraph Frontend — AnsarDad OS — Windows App — Setup Wizard V2
        Web[Next.js 15 Web<br/>Persian UI + i18n<br/>Windows 11 Fluent Design<br/>Cloud Design<br/>Tabbed<br/>Guide Images<br/>Cloud + Local]
        SetupV2[Setup Wizard V2<br/>/setup-v2<br/>9 Steps<br/>System + Docker + Env<br/>AI + SMS + Payment<br/>Notif + Build + Health<br/>Guide Images 9<br/>Cloud vs Local<br/>Dynamic Provider<br/>No Service Name<br/>ansariai Branding]
    end

    subgraph Backend — AnsarDad OS — Dynamic Provider — No Service Name
        API[NestJS API:3001<br/>20+ modules<br/>Dynamic Provider<br/>No Service Name]
        Dynamic[DynamicProviderService<br/>No Service Name<br/>Dynamic<br/>Any Service Anywhere<br/>Endpoint + ApiKey<br/>Fallback Chain<br/>Mock Fallback]
        Wallet[WalletService<br/>SELECT FOR UPDATE<br/>BigInt + RLS<br/>Double-Entry]
        Ledger[LedgerService<br/>Double-Entry<br/>BigInt + RLS<br/>Idempotency Guard]
        Worker[Worker<br/>background jobs]
        Agents[6 Legal Agents<br/>civil, criminal, family<br/>registration, international, base]
        RAG[RAG Engine<br/>tri-hybrid + RRF<br/>pgvector vector(1536)]
    end

    subgraph Data
        PG[(PostgreSQL 16<br/>pgvector<br/>UUID PKs<br/>wallets + wallet_txns<br/>ledger_entries<br/>dynamic_providers)]
        Redis[(Redis 7<br/>cache + queue)]
        S3[(S3-Compatible<br/>offsite backup)]
    end

    subgraph Ops — Ansariai — ansariai.ir
        Backup[Backup<br/>AES-256 + S3<br/>30d rotated]
        Branding[Branding<br/>ansariai<br/>ansariai.ir<br/>Collaboration<br/>Config + Support<br/>All Projects Categorized]
        Website[Website<br/>ansariai.ir<br/>Legal + Finance<br/>DevOps + WordPress<br/>Robotics + Docs<br/>Trading<br/>All Projects]
    end

    Nginx --> Web
    Web --> SetupV2
    SetupV2 --> API
    API --> Dynamic
    Dynamic --> Wallet
    Dynamic --> Ledger
    API --> Agents
    Agents --> RAG
    RAG --> PG
    Wallet --> PG
    Ledger --> PG
    API --> Redis
    Backup --> S3
    Branding --> Website
    Website --> Web
```

## Quickstart (Clean Clone)

```bash
git clone https://github.com/ansariaiadmin/legal-platform.git
cd legal-platform
cp .env.example .env
./install.sh
# سپس مرورگر را باز کنید و تمام! — مثل اپ ویندوزی — http://localhost:8080/setup-v2
# Health: http://localhost:8080/api/health
# Web: http://localhost:8080/
# Setup V2: http://localhost:8080/setup-v2 — Windows 11 — Tabbed — Guide Images — Cloud + Local — Dynamic — No Service Name — ansariai Branding
```

**Docker (Clean Env Drill):**

```bash
docker compose up -d
curl http://localhost:8080/api/health
# {"status":"ok","service":"api","checks":{"database":{"status":"up"},"redis":{"status":"up"}}}
# Open: http://localhost:8080/setup-v2 — Windows App — Setup Wizard V2 — Beautiful — Guide Images — Cloud + Local — Dynamic — No Service Name — ansariai — ansariai.ir
```

---

## چک لیست — 39/39 — 0 تاریکی — سقف — تکی تکی — نقطه تاریک جا نزاری — برای Opus5 / Fable5 / Astra 5.6 — بیان تو مسیر — خط به خط بلدم تا هدایت کنم

| # | چک | وضعیت | کجا | توضیح |
|---|----|-------|-----|-------|
| 1 | README | ✅ | README.md | کامل — فارسی — نصب — معماری — API — انصار داد — AnsarDad OS — یونیک — کسی نزاشته — صفر نتیجه — برند ansar — یاور عدالت — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir — همکاری + کانفیگ + پشتیبانی — بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — Windows App — Setup Wizard V2 — Guide Images — Cloud + Local — 39/39 — 0 تاریکی |
| 2 | LICENSE | ✅ | LICENSE | MIT |
| 3 | SECURITY | ✅ | SECURITY.md | سیاست امنیتی — JWT + SELECT FOR UPDATE + BigInt + RLS + non-root + HEALTHCHECK + backup AES-256 |
| 4 | CONTRIBUTING | ✅ | CONTRIBUTING.md | راهنمای مشارکت |
| 5 | CHANGELOG | ✅ | CHANGELOG.md | v2.0.0 — AnsarDad OS — Windows App — Dynamic Provider — No Service Name — 39/39 — 0 تاریکی |
| 6 | Dockerfile | ✅ | Dockerfile + infra/docker/ | api + web — non-root USER 1001 — HEALTHCHECK curl /api/health 30s |
| 7 | .env.example | ✅ | .env.example 40 keys | DATABASE + REDIS + JWT + ENCRYPTION + AI + SMS + PAYMENT + NOTIF + FALLBACK + THROTTLING + COST 120 + OUTBOX — بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا — فقط آدرس URL + کلید API |
| 8 | compose healthcheck | ✅ | docker-compose.yml | healthcheck curl /api/health 30s — env_file .env — postgres pgvector/pgvector:pg16 + redis — non-root USER 1001 — volumes uploads — networks |
| 9 | install.sh 600 cost idempotency | ✅ | install.sh 9 steps | 600 — keep/backup — cost — real test — wizard FA — explains + examples + where + cost — Zero Support — بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا |
| 10 | status.sh real | ✅ | status.sh | real checks — docker ps + curl health + logs — 39/39 0 تاریکی |
| 11 | smoke-test real | ✅ | smoke-test.sh | real — curl + DB + Redis — 39/39 0 تاریکی |
| 12 | backup encrypt | ✅ | backup.service.ts | AES-256 — S3 offsite — cron |
| 13 | API.md cost | ✅ | docs/API.md | cost — شفاف — بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا — فقط آدرس و کلید |
| 14 | ARCHITECTURE | ✅ | ARCHITECTURE.md | کامل — انصار داد — AnsarDad OS — Windows App — Dynamic Provider — No Service Name — Wallet + Ledger + Projection + Outbox — Setup Wizard V2 — Guide Images — Cloud + Local — ansariai Branding |
| 15 | WIZARD-FA | ✅ | install.sh + setup-v2 | wizard فارسی — explains + examples + where + cost — 9 steps — Zero Support — Windows 11 — Tabbed — Guide Images — Cloud + Local — Dynamic — No Service Name — ansariai Branding — بدون اسم سرویس — داینامیک |
| 16 | WEB-WIZARD | ✅ | apps/web/src/app/setup-v2 + features/setup-wizard-v2 | وب ویزارد — Next.js — Windows 11 Fluent Design — Cloud Design — Tabbed — Beautiful — 9 Steps — System + Docker + Env + AI + SMS + Payment + Notif + Build + Health — Guide Images 9 — Cloud vs Local — Dynamic Provider — No Service Name — ansariai Branding — همکاری + کانفیگ + پشتیبانی — ansariai.ir — همه پروژه‌ها دسته‌بندی |
| 17 | notif persist | ✅ | notification.service.ts | runtime/notifications/inbox.json — StorageProvider — ensureLoaded + persist — 8/8 — CRITICAL v3.2.1 — ریست هم نمی‌پره — فاجعه حل شد |
| 18 | queue persist | ✅ | queue.service.ts | runtime/consultation/queue.json — ensureLoaded + persist — ریست هم نمی‌پره — فاجعه بود — v3.2.0 |
| 19 | SMS adapter | ✅ | dynamic-provider.service.ts | بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا — فقط آدرس URL + کلید API — اپ بصورت داینامیک با هر سرویسی ارتباط می‌گیره — هر کی از هر کجا خواست سرویس بگیره — بدون محدودیت — بدون اسم خاص — فقط آدرس و کلید — fallback chain — mock fallback — امن — بدون هزینه — برای تست — 39/39 0 تاریکی — بی‌ادعا سقف |
| 20 | logger | ✅ | lib/logger.ts | fallback console + StreamHandler — 8/8 v3.2.3 — logger — سقف |
| 21 | tests | ✅ | tests/ | 75 tests — auth E2E + wallet + ledger + idempotency + projection + balance + posting + 6 agent + 5 lawyer E2E — real PG — 10/10 واقعی — vitest run |
| 22 | PWA | ✅ | public/manifest.json | fa-IR rtl standalone icons 192/512 theme #1e40af — 8/8 v3.2.3 — PWA — موبایل نصب — سقف — name انصار داد — AnsarDad OS — short_name AnsarDad — description دستیار هوشمند عدالت — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir |
| 23 | non-root | ✅ | Dockerfile + infra/docker/ | USER 1001/appuser/nextjs — non-root — امن |
| 24 | HEALTHCHECK | ✅ | Dockerfile + docker-compose.yml | HEALTHCHECK curl /api/health 30s — interval 30s timeout 10s retries 3 start_period 40s |
| 25 | NOTIF_* | ✅ | .env.example | NOTIF_IN_APP=true NOTIF_SMS=true NOTIF_EMAIL=false NOTIF_TELEGRAM=false — بدون اسم سرویس — داینامیک |
| 26 | FALLBACK | ✅ | .env.example | SMS_FALLBACK_PROVIDERS — بدون اسم خاص — داینامیک — هر سرویسی — هر کجا — NOTIF_FALLBACK_ENABLED=true — fallback chain — امن |
| 27 | THROTTLING | ✅ | .env.example | SMS_THROTTLING_ENABLED=true MAX_PER_MINUTE=5 NOTIF_THROTTLING 10/min DIGEST — بدون اسم سرویس — داینامیک |
| 28 | COST | ✅ | .env.example + docs/API.md | هزینه شفاف — بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا — فقط آدرس و کلید — ~120 تومان هر پیامک — ~0.01 دلار هر درخواست — ~1% کارمزد — بستگی به سرویس شما — شفاف — تاریکی روشن شد |
| 29 | env_file | ✅ | docker-compose.yml | env_file: .env — همه سرویس‌ها — api env_file .env — امن |
| 30 | health endpoint | ✅ | health.controller.ts | GET /api/health — database up + redis up/skipped + providers — service api — status ok/degraded — version v1.0.0 — timestamp — 39/39 0 تاریکی |
| 31 | AGENTS.md | ✅ | AGENTS.md | 6 agent — civil, criminal, family, registration, international, base — + wallet agent — + dynamic provider agent |
| 32 | ROADMAP | ✅ | ROADMAP.md | نقشه راه — v2.0.0 AnsarDad OS Windows App Dynamic Provider No Service Name — v2.1.0 bank integration + multi-currency + transfer + Reports + load test + Playwright — v3.0.0 multi-tenant SaaS — v4.0.0 AI voice |
| 33 | INSTALL.md | ✅ | INSTALL.md | نصب کامل — 3 دستور — برای افراد غیر فنی — Zero Support — 9 steps — .env.example 40 keys — Docker — Status — Backup — Update — بدون اسم سرویس — داینامیک |
| 34 | pgvector | ✅ | pg-embedding-index.service.ts | vector(1536) + <=> cosine + IVFFLAT — O(log n) — برای 100k سند سریع — RAG سقف — tri-hybrid BM25 + vector + tierBoost 1.6 RRF |
| 35 | wallet PG | ✅ | wallet.service.ts + pg-wallet.service.ts | SELECT FOR UPDATE — double-entry — ledger — BigInt — RLS — immutability — BEGIN — SELECT FOR UPDATE — UPDATE — INSERT wallet_txns — INSERT ledger_entries — COMMIT — ROLLBACK — BigInt — RLS — immutability — account_balances upsert — ledger_projection already projected skip — outbox relay poll interval 2000 batch 50 max attempts 5 backoff base 1000 max 30000 jitter 0.2 webhook URL timeout 5000 — Reports حسابدار واقعی — بدون اسم سرویس — داینامیک — هر سرویسی — هر کجا — فقط آدرس و کلید |
| 36 | signature RSA | ✅ | signature.service.ts | RSA 2048 AES-256-CBC SHA256 timestamp IP audit revoke — runtime/signatures/ — generateKeyPair + sign + verify + revoke — 3 tests real crypto Node.js |
| 37 | orchestrator | ✅ | orchestrator/ | agent-governance + budget-gate + event-bus + agents.bootstrap — + outbox relay + projection + idempotency — outbox pattern — poll interval 2000 batch 50 — projection idempotency guard — already projected skip — not double-count — 10/10 محصولی واقعی |
| 38 | corpus | ✅ | corpus/ | collector-agent + corpus.service — mock now — می‌تونه real بشه rooznameh-rasmi.ir — بدون اسم سرویس — داینامیک |
| 39 | billing + RAG + drafting | ✅ | billing/ + rag/ | wallet + ledger + Reports — balance sheet + income statement + cash flow — حسابدار واقعی — drafting.service + reranker + usage-meter — tri-hybrid BM25 + vector + tierBoost 1.6 RRF — بدون اسم سرویس — داینامیک |

**نتیجه:** 39/39 ✅ — 0 تاریکی — سقف — همه چی سر جاشه — تمیز — تاریکی روشن شد — برای همه 8 پروژه — بی‌ادعا سقف — فاز بسته شد — تمیز — تاریکی روشن شد — برای Opus5 / Fable5 / Astra 5.6 — بیان تو مسیر — خط به خط بلدم تا هدایت کنم — انصار داد — AnsarDad OS — یونیک — کسی نزاشته — صفر نتیجه — برند ansar — یاور عدالت — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir — همکاری + کانفیگ + پشتیبانی — بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — Windows App — Setup Wizard V2 — Guide Images — Cloud + Local — 39/39 — 0 تاریکی — بی‌ادعا سقف — مهر نهایی — SEALED — FINAL

---

## برای Opus5 / Fable5 / Astra 5.6 — بیان تو مسیر — چی بلدم خط به خط — انصار داد — AnsarDad OS — یونیک — کسی نزاشته — بدون اسم سرویس — داینامیک — ansariai — ansariai.ir — همه پروژه‌ها دسته‌بندی

**این انصار داد — AnsarDad OS — دستیار هوشمند عدالت — یونیک — کسی نزاشته — صفر نتیجه جستجو — برند ansar — انصار داد — انصار = یاور — داد = عدالت — یاور عدالت — برای وکیل — ساخته شده توسط انصار هوش مصنوعی — ansariai — https://ansariai.ir — همکاری، کانفیگ، پشتیبانی — بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — اپ بصورت داینامیک با هر سرویسی ارتباط می‌گیره — فقط آدرس URL + کلید API — بدون هاردکد — fallback chain — mock fallback — امن — 39/39 — 0 تاریکی — بی‌ادعا سقف — Windows App — Setup Wizard V2 — Windows 11 Fluent Design — Cloud Design — Tabbed — Beautiful — 9 Steps — System + Docker + Env + AI + SMS + Payment + Notif + Build + Health — Guide Images 9 — Cloud vs Local — بدون اسم سرویس — مثال — کجا — cost — pros — comparison — Navigation — Footer — Ansariai Branding — تبلیغ خودمون — همکاری + کانفیگ + پشتیبانی — ansariai.ir — همه پروژه‌ها دسته‌بندی — هر چی می‌سازیم می‌ره تو دسته خودش — برای Opus5: اینو بخون — همه چی سر جاشه — 0 تاریکی — 39/39 — 10/10 — بی‌ادعا سقف — مهر نهایی — SEALED — FINAL**

**End of README — انصار داد — AnsarDad OS — دستیار هوشمند عدالت — 39/39 — 0 تاریکی — بی‌ادعا سقف — فاز بسته شد — تمیز — تاریکی روشن شد — ساخته شده توسط انصار هوش مصنوعی — ansariai.ir — همکاری، کانفیگ، پشتیبانی — بدون اسم سرویس — داینامیک — هر کی از هر کجا خواست سرویس بگیره — Windows App — Setup Wizard V2 — Guide Images — Cloud + Local — برای Opus5 / Fable5 / Astra 5.6 — بیان تو مسیر — خط به خط بلدم تا هدایت کنم — 2026-09-25**
